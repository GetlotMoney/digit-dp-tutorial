# 4. 经典入门题

以下题目用基本模板稍作改动即可解决，建议按顺序做，每题都先自己想状态再对照。

## 4.1 数字之和类

例如「求 `[L,R]` 各位数字之和等于某定值的数个数」。额外状态加一个 `sum`，边界时判断 `sum == target`。

> **TODO** [待补充] 题目链接、你的代码、思路笔记

## 4.2 不含某子串（如不含 62）

经典题 HDU 2089。额外状态加一个 `pre` 表示上一位填的数字，转移时排除 `pre==6 && d==2`。

> **TODO** [待补充] 题目、代码、笔记

## 4.3 数字出现次数

例如洛谷 P2602，统计每个数字 0-9 在 `[L,R]` 中出现的总次数。对每个数字分别跑一遍数位 DP，额外状态记 `cnt`。

> **TODO** [待补充] 题目、代码、笔记
