# 1. 基础知识回顾

在进入数位 DP 之前，确保你对以下内容足够熟悉，它们是理解数位 DP 的基石。

## 1.1 记忆化搜索

数位 DP 最常见的实现方式就是记忆化搜索（DFS + memo）。回顾一下：用一个 `dp[...]` 数组缓存已计算过的状态，下次遇到相同状态直接返回。

> **TODO** [待补充] 记忆化搜索的回顾示例代码 —— 你可以放一个经典例子（如斐波那契、滑雪）

## 1.2 前缀和思想 · 交互演示

大多数数位 DP 题问的是「区间 `[L, R]` 内满足条件的数有多少个」。我们转化为 `f(R) - f(L-1)`，其中 `f(N)` 表示 `[0, N]`（含 0）内满足条件的个数。下面的演示帮你直观看到这个转化。

<!--DEMO:prefixsum-->

## 1.3 状态设计回顾

DP 的核心永远是「状态 + 转移」。数位 DP 的状态里，除了题目本身的条件，还要额外维护「数位位置」「是否受上界限制」「是否有前导零」这几个维度。
